# 코스트코 제품 라벨 자동 OCR → 구글 시트 시스템 (Azure Read API 버전)

> 처음 이 저장소를 맡으셨다면 `HANDOFF.md`를 먼저 읽어주세요 — 전체 그림과
> 지금 뭘 해야 하는지가 정리되어 있습니다. 이 문서(README)는 시스템이 어떻게
> 동작하는지에 대한 상세 레퍼런스입니다.

Google Drive 폴더에 코스트코 매대 가격표 사진을 대량(예: 월 1,500장) 업로드하면
Azure AI Vision의 Read API로 무료 OCR을 돌려서, 상품코드/제품명(한국어)/가격만 뽑아
Google Sheets에 자동으로 정리해주는 시스템입니다. **대시보드의 "업데이트" 버튼을
누르면 그 시점에 실행**됩니다 (GitHub Actions의 `workflow_dispatch` API를 대시보드
백엔드가 호출하는 방식 - 연동 방법은 `DASHBOARD_INTEGRATION.md` 참고). 언제 사진이
올라올지 알 수 없는 상황에서 굳이 주기적으로 폴링할 필요가 없어서, 필요한 시점에만
실행되도록 했습니다. 서버 운영 비용 없이 완전 무료로 동작합니다.

## 왜 Azure Read API인가?

- 무료 티어(F0): **월 5,000건까지 완전 무료**, 월 1,500장 사용에는 비용이 전혀 들지 않습니다.
- 단, 무료 티어는 **분당 20건 속도 제한**이 있습니다. 1,500장 처리 시 약 75~90분 소요됩니다.
  (사람이 지켜볼 필요 없는 배치 작업이므로 문제되지 않습니다.)
- 유료로 전환하면 속도 제한이 크게 완화되지만, 지금은 무료 유지가 목표이므로 F0로 충분합니다.

## 왜 GitHub Actions인가?

- 서버를 따로 띄우지 않아도 **REST API 호출 한 번으로 실행을 트리거**할 수 있습니다
  (Private 저장소도 월 2,000분 무료 — 새 파일이 없으면 몇 초 만에 끝나는 실행이라 크게
  소모되지 않습니다. 사진을 한꺼번에 많이 올린 달에는 OCR 처리에 수십~90분 정도 쓰입니다).
- 처리 이력은 로컬 파일이 아니라 **구글 시트 자체**(파일ID 열)를 기준으로 판단하므로,
  매번 새로 뜨는 Actions 실행 환경에서도 중복 처리 없이 이어서 작동합니다.
- 워크플로에 `concurrency` 설정이 있어서, 이전 실행이 아직 진행 중일 때(대량 업로드
  직후 OCR이 오래 걸리는 경우) 버튼이 또 눌려도 두 실행이 겹치지 않고 순서대로 돕니다.

## 1. Azure 리소스 생성

1. https://portal.azure.com 접속 (없으면 무료 계정 생성, 카드 등록 필요하나 F0는 과금 없음)
2. "리소스 만들기" → "Azure AI Vision" 검색 → 만들기
3. 가격 책정 계층(Pricing tier)에서 **F0 (무료)** 선택 (구독당 F0 리소스는 1개만 생성 가능)
4. 생성 완료 후 리소스의 "키 및 엔드포인트" 메뉴에서 다음 두 값을 복사:
   - `AZURE_VISION_ENDPOINT` (예: `https://your-resource.cognitiveservices.azure.com`)
   - `AZURE_VISION_KEY`

## 2. Google 쪽 설정

1. [Google Cloud Console](https://console.cloud.google.com)에서 프로젝트 생성 후 **Drive API**, **Sheets API** 활성화
2. "API 및 서비스 → 사용자 인증 정보 → 서비스 계정 만들기"로 서비스 계정 생성 → JSON 키 다운로드
3. 결과를 저장할 Google Sheet를 하나 만들고, 다운로드한 JSON 안의 `client_email` 값을
   해당 시트에 **편집자**로 공유.
4. **Drive 폴더를 코스트코용/트레이더스용 두 개로 따로 만듭니다** (예: "코스트코 사진",
   "트레이더스 사진"). 사진 형식을 텍스트로 추측하지 않고 어느 폴더에 넣었는지로
   바로 판별하기 위해서입니다 - 코스트코 사진은 코스트코 폴더에, 트레이더스 사진은
   트레이더스 폴더에 넣어야 합니다. 두 폴더 모두 서비스 계정 이메일을 **편집자**로
   공유해야 합니다(처리 완료 사진을 하위 폴더로 이동하려면 뷰어 권한으로는 부족합니다).
5. 시트 URL에서 `SPREADSHEET_ID`, 두 Drive 폴더 URL에서 각각
   `DRIVE_FOLDER_ID_COSTCO` / `DRIVE_FOLDER_ID_TRADERS` 확인
   (`https://docs.google.com/spreadsheets/d/여기부분/edit`, `https://drive.google.com/drive/folders/여기부분`)

## 3. 로컬에서 테스트 실행

```bash
pip install -r requirements.txt
cp .env.example .env
# .env 파일에 DRIVE_FOLDER_ID_COSTCO, DRIVE_FOLDER_ID_TRADERS, SPREADSHEET_ID,
# AZURE_VISION_ENDPOINT, AZURE_VISION_KEY 채우기
# 다운로드한 서비스 계정 JSON을 service_account.json 이름으로 이 폴더에 저장

python main.py
```

## 4. GitHub Actions 실행 설정

저장소 **Settings → Secrets and variables → Actions → New repository secret**에서 아래 값을 등록하세요.

| Secret 이름 | 값 |
|---|---|
| `DRIVE_FOLDER_ID_COSTCO` | 코스트코 사진 전용 Drive 폴더 ID |
| `DRIVE_FOLDER_ID_TRADERS` | 트레이더스 사진 전용 Drive 폴더 ID |
| `SPREADSHEET_ID` | 구글 시트 ID |
| `SHEET_NAME` | 코스트코 시트 탭 이름 (예: `시트1`) |
| `TRADERS_SHEET_NAME` | 트레이더스 시트 탭 이름 (미설정 시 기본값 `트레이더스`) |
| `CATEGORY_SHEET_NAME_COSTCO` | 코스트코 제품군정리 시트 탭 이름 (미설정 시 기본값 `제품군정리(코스트코)`) |
| `CATEGORY_SHEET_NAME_TRADERS` | 트레이더스 제품군정리 시트 탭 이름 (미설정 시 기본값 `제품군정리(트레이더스)`) |
| `AZURE_VISION_ENDPOINT` | Azure 엔드포인트 URL |
| `AZURE_VISION_KEY` | Azure 키 |
| `GOOGLE_SERVICE_ACCOUNT_JSON` | 서비스 계정 JSON 파일의 **내용 전체**를 그대로 복사해서 붙여넣기 |
| `MC_CATALOG_SPREADSHEET_ID` | (선택) 자사 상품 카탈로그 구글 시트 ID — 7절 참고 |
| `MC_CATALOG_SHEET_NAME` | (선택) 카탈로그 시트 탭 이름 (미설정 시 기본값 `시트5`) |

### 자사 카탈로그 시트 준비 (제품군 자동 분류 정확도를 높이고 싶다면)

`MC_CATALOG_SPREADSHEET_ID`는 **이 저장소와는 별개의, 비공개 구글 시트**를
가리켜야 합니다. 이 저장소(`costco-label-ocr`)는 **Public**이라 카탈로그
데이터(거래처/공급사 이름 등 사외비 정보 포함)를 여기 코드나 파일로 직접
넣으면 안 됩니다.

1. 새 구글 시트를 만들고(기존 결과 시트와는 다른, 별도의 시트), `MCH1` /
   `MC` / `상품명` 3개 컬럼으로 자사 상품 카탈로그를 채웁니다 (`MCH1`은 대분류라
   실제 분류에는 안 쓰이지만 있어도 무방합니다. `MC`가 제품군, `상품명`이
   그 제품군에 속한 실제 SKU명입니다).
2. 이 시트를 서비스 계정 이메일(`GOOGLE_SERVICE_ACCOUNT_JSON`의 `client_email`)에
   **뷰어**로 공유합니다 (읽기만 하므로 편집자 권한은 필요 없습니다).
3. 시트 URL에서 `MC_CATALOG_SPREADSHEET_ID`를 확인해 시크릿으로 등록합니다.
   탭 이름이 `시트5`가 아니면 `MC_CATALOG_SHEET_NAME`도 같이 등록하세요.

설정하지 않아도 파이프라인은 정상 동작합니다 — 이 경우 제품군 분류는
`CATEGORY_KEYWORDS`(코드에 있는 수동 목록)만으로 이뤄집니다. 자세한 매칭
방식은 7절 참고.

등록 후에는 `.github/workflows/auto-ocr.yml`의 `workflow_dispatch` 트리거로 실행됩니다.
자동 스케줄(크론)은 없고, 아래 두 방법 중 하나로 실행을 트리거합니다:

- **대시보드 "업데이트" 버튼**: 대시보드 백엔드가 GitHub REST API를 호출 (연동 방법은
  `DASHBOARD_INTEGRATION.md` 참고)
- **수동 확인용**: 저장소의 **Actions 탭 → 코스트코 라벨 OCR 실행 (대시보드 트리거 / 수동 실행)
  → Run workflow**

## 5. 동작 방식

- 코스트코/트레이더스 두 Drive 폴더를 각각 전부 조회(페이지네이션 처리, 1,500장도
  누락 없이 조회)하고, 각 폴더에 대응하는 구글 시트의 `파일ID` 열에 이미 기록된
  파일은 건너뜁니다. 새로 발견된 파일이라도 같은 날 촬영된 완전히 동일한 사진이
  이미 등록되어 있으면(`사진해시` 기준, 아래 6절 참고) OCR 없이 건너뜁니다.
- 신규 이미지만 병렬로 다운로드하고, Azure 속도 제한(분당 18건, 여유 있게 설정)에 맞춰
  순차적으로 OCR을 호출합니다.
- 일시적 오류(네트워크 문제, 429 속도 제한 등)는 자동 재시도됩니다. 재시도 후에도 실패한
  파일은 시트에 기록되지 않으므로 다음 실행 때 자동으로 다시 시도됩니다.
- **사진 한 장당 항상 한 행만 추출합니다.** 프레임 안에 배경 진열대의 다른
  가격표가 흐릿하게 같이 찍혀도, 그 안에서 처음 인식되는 상품(주로 가장 크게/
  가장 먼저 나오는 목표 가격표)만 뽑고 나머지는 무시합니다. 상품을 여러 개
  등록하고 싶으면 사진을 상품별로 각각 따로 찍어서 올려주세요.
- **코스트코/트레이더스는 사진을 넣은 Drive 폴더로 구분합니다.** 예전에는 한
  폴더에 섞어 올리고 텍스트 내용(상품코드가 맨 앞 줄에 오는지 등)으로 자동
  판별했지만, 사진마다 레이아웃이 조금씩 달라서 오분류가 잦았습니다. 지금은
  `DRIVE_FOLDER_ID_COSTCO` 폴더에 넣은 사진은 무조건 코스트코 파서로, 
  `DRIVE_FOLDER_ID_TRADERS` 폴더에 넣은 사진은 무조건 트레이더스 파서로
  처리되어 오분류 자체가 구조적으로 발생할 수 없습니다. 코스트코 사진은
  `SHEET_NAME` 탭에, 트레이더스 사진은 `TRADERS_SHEET_NAME` 탭에 기록됩니다.
  두 탭 모두 없으면 자동으로 만들어집니다. 중복 처리 방지용 파일ID 체크는
  각 폴더/시트 안에서 따로 봅니다(폴더가 이미 분리되어 있으므로 두 탭을 합쳐
  볼 필요가 없습니다).
- **시트 기록이 끝난 사진은 삭제하지 않고 같은 Drive 폴더 안 `처리완료` 하위
  폴더로 이동합니다.** 원본은 그대로 보존되어 나중에 재검증이 필요하면 다시
  볼 수 있고, 신규 업로드 폴더(코스트코/트레이더스 각각)는 처리된 사진이
  빠지면서 계속 가벼운 상태로 유지되어 폴더 조회 속도가 데이터 누적과 무관하게
  일정합니다. 이 이동을 하려면 서비스 계정이 두 Drive 폴더 모두에 최소
  **편집자** 권한으로 공유되어 있어야 합니다(뷰어 권한만으로는 이동이 실패하며,
  실패해도 시트 기록은 이미 끝난 상태라 데이터 유실은 아니고 그 사진만 원래
  폴더에 남습니다).
- **소량 테스트나 특정 폴더만 처리하고 싶을 때**는 `workflow_dispatch` 호출
  시 `costco_folder_id` / `traders_folder_id` 입력값으로 각각 다른 Drive 폴더
  ID를 넘기면, 그 실행에서만 해당 시크릿 대신 그 폴더를 사용합니다(시크릿 값
  자체는 바뀌지 않음). 둘 다 비워두면 평소처럼 기본 폴더를 사용하고, 한쪽만
  채우면 그 리테일러만 다른 폴더로 테스트할 수 있습니다.
- **원본 시트(아래 6절)와 별개로, 제품군 단위로 정리된 시트도 같이
  갱신됩니다** (자세한 내용은 7절 참고).

## 6. 시트 컬럼 구조

**핵심 컬럼** (식품/비식품 가릴 것 없이 모든 SKU에 공통으로 채워짐):
`파일ID`, `파일명`, `처리일시`, `원문텍스트`, `상품코드`, `제품명(한국어)`, `가격`

**비핵심 컬럼** (사진에 그 정보가 있으면 채우고, 없으면 빈 칸):
`제품명(영어)`, `중량`, `단가`

**앞뒤 매칭 컬럼** (제품 뒷면 사진도 포함해 모든 행에 채워짐):
`업로더`, `뒷면여부`, `촬영시각`, `사진해시`, `매칭파일ID`, `정리본위치`

핵심 컬럼을 앞에, 비핵심 컬럼을 뒤에, 앞뒤 매칭 컬럼을 맨 뒤에 두는 순서입니다.
식품/비식품을 나누는 기준으로 컬럼을 다르게 쓰지 않고, 사진마다 있는 정보만
그때그때 채웁니다.

`파일ID`는 중복 처리 방지를 위한 식별자이며, `원문텍스트`에는 항목 파싱과 무관하게
OCR로 읽은 전체 텍스트가 그대로 남아있어 파싱이 잘못된 경우 원문 대조가 가능합니다.

`파일ID`는 Drive가 업로드마다 새로 부여하는 값이라, **내용이 완전히 같은 사진 파일을
실수로 두 번 업로드해도 서로 다른 `파일ID`를 받아 각각 신규로 처리됩니다.** 이걸
잡아내기 위한 값이 `사진해시`(사진 원본 바이트의 SHA-256)입니다 - 같은 날 촬영된
사진 중 `사진해시`와 촬영 날짜가 둘 다 이미 기록된 값과 일치하면 그 사진은 OCR
없이 건너뛰고 바로 `처리완료` 폴더로 옮깁니다. **날짜가 다르면 해시가 같아도
걸리지 않습니다** - 예를 들어 같은 상품을 다음 달에 다시 촬영해서 올리면(심지어
우연히 이전 사진과 똑같이 나와도) 새 행으로 정상 등록됩니다. 시기별로 "그 달에
어떤 상품이 있었는지" 이력을 쌓으려는 목적과, 실수로 같은 사진을 두 번 올리는
경우를 거르려는 목적이 서로 충돌하지 않도록 날짜까지 같아야만 중복으로 봅니다.
촬영시각을 알 수 없는 사진(EXIF도 파일명 패턴도 없음)은 "같은 날"을 판단할
근거가 없으므로 이 검사 대상에서 빠지고 항상 신규로 처리됩니다.

`업로더`는 그 사진을 올린 사람의 Drive 계정 이메일, `뒷면여부`는 상품카드/제품
뒷면 중 어느 쪽으로 판별됐는지(뒷면이면 "뒷면", 카드면 빈 칸), `촬영시각`은 EXIF
촬영시각(없으면 파일명의 날짜_시각 패턴)입니다. `매칭파일ID`는 같은 사람이 촬영
순서대로 카드 다음에 바로 뒷면을 올렸을 때 서로의 `파일ID`를 채워주는 컬럼으로,
카드 행엔 매칭된 뒷면 파일ID(여러 장이면 쉼표로 나열), 뒷면 행엔 매칭된 카드의
파일ID가 들어갑니다.

이 파이프라인은 정해진 시각에 자동으로 도는 게 아니라 대시보드 "업데이트" 버튼을
누를 때마다 한 번씩 실행되므로, 카드와 뒷면이 서로 다른 실행 회차에 나뉘어 올라올
수 있습니다(뒷면을 깜빡하고 나중에 올리는 등). 그래서 매칭은 이번 실행에서 새로
처리된 사진끼리만 비교하지 않고, **실행할 때마다 RAW 시트에 지금까지 쌓인 모든
행을 다시 훑어 재계산**합니다 - 뒤늦게 올라온 뒷면(또는 카드)도 다음 "업데이트"
클릭 때 과거 행까지 소급 매칭됩니다. 다만 `업로더`/`촬영시각` 컬럼이 도입되기
전에 이미 처리된 과거 행은 이 값이 비어있어 매칭 대상이 될 수 없습니다 - 도입
이후 처리되는 사진부터 적용됩니다.

업로더 정보가 없거나(공유 드라이브처럼 파일별 소유자가 없는 구조 등) 촬영시각을
알 수 없는 사진, 또는 순서상 앞뒤 짝이 없는 사진은 `매칭파일ID`가 빈 칸으로
남습니다 - 매칭 실패일 뿐 데이터 자체는 그대로 기록됩니다.

`정리본위치`는 상품카드 행에만 채워지며, 그 카드가 어느 제품군정리 탭의 어느
칸("정리본(코스트코)_2026-07!C15"처럼 탭 제목+열+제목행)에 기록됐는지를
나타냅니다. 제품군정리 탭이 촬영월별로 나뉘어 있어(7절) 탭 제목까지 같이
남겨야, 카드보다 늦게(심지어 몇 달 뒤에) 올라오는 뒷면 사진의 소싱형태/
병입원산지를 정확한 탭·칸에 소급 반영할 수 있습니다. 제품군정리 탭 자체에는
추적용 ID를 두지 않는다는 원칙(7절)은 그대로 유지됩니다.

`가격`은 정가/할인가가 같이 찍혀있어도 **항상 정가**를 선택합니다 ("할인행사" 문구나
"-4,500원" 같은 할인폭 표시가 나오면 그 이후에 나오는 가격은 절대 선택하지 않음).
`단가`는 "217원/10g"처럼 기준 단위까지 같이 기록해서, 몇 g당 가격인지 알 수 있게
합니다. 단가로 쓴 줄은 가격 후보에서 제외하므로 같은 값이 두 컬럼에 중복되지
않습니다.

`중량`은 "50G X 12", "1.2KG", "1.2kgx1개"처럼 숫자+단위(g/ml/kg/l/m) 패턴이면
공백 없이 곱셈 표시나 한글이 바로 붙어있어도 인식합니다.

## 7. 제품군정리 시트

코스트코/트레이더스 원본 시트(위 6절)와는 별개로, 같은 스프레드시트 안에
`MONTHLY_CATEGORY_SHEET_NAME_COSTCO`(기본값 `정리본(코스트코)`)와
`MONTHLY_CATEGORY_SHEET_NAME_TRADERS`(기본값 `정리본(트레이더스)`) 이름 뒤에
**촬영월(`_2026-07`처럼 `YYYY-MM`)을 붙인 탭이 매달 하나씩 새로 생깁니다**
(예: `정리본(코스트코)_2026-07`, `정리본(코스트코)_2026-08`) —
원본 시트가 코스트코/트레이더스로 나뉘어 있는 것과 마찬가지로, 상품 성격이
서로 달라서 제품군 정리도 리테일러별로 따로 두고, 여기에 더해 **"몇 월에
어떤 상품이 있었는지" 스냅샷을 보여주기 위해 달별로도 나눕니다.** 어느
달 탭에 들어가는지는 OCR을 돌린 시점이 아니라 **사진이 실제로 촬영된
날짜**(`촬영시각`) 기준입니다 - 그래서 같은 상품을 다음 달에 다시 촬영해서
올리면 중복 제거되지 않고 그 달의 탭에 새 열로 쌓입니다(반대로, 같은 날
찍힌 완전히 동일한 사진의 실수 재업로드는 위 6절의 `사진해시` 검사로 애초에
걸러집니다). 촬영시각을 알 수 없는 사진만 예외적으로 처리(OCR 실행)된
달의 탭에 들어갑니다. 카메라 시계가 안 맞은 채로 찍힌 사진(EXIF/파일명에
찍힌 날짜 자체가 틀림 - 예: 배터리가 다 됐다 충전한 폰이 날짜를 리셋값으로
찍는 경우)은 코드가 감지할 방법이 없어 그 틀린 날짜 기준으로 탭이 만들어질
수 있습니다 - 이런 경우를 발견하면 해당 탭 이름을 수동으로 고치고, 그
탭을 참조하는 RAW 시트의 `정리본위치` 값들도 같이 고쳐줘야 합니다.

`CATEGORY_SHEET_NAME_COSTCO`/`CATEGORY_SHEET_NAME_TRADERS`(기본값
`제품군정리(코스트코)`/`제품군정리(트레이더스)`)는 이름이 비슷해 보이지만
다른 용도입니다 - 월별 분리 기능이 생기기 전까지 쓰던 옛 단일 탭
이름이고, 그 시절 데이터와 `정리본위치` 형식이 여전히 이 이름을 참조하므로
(`sync_back_sourcing()` 참고) 그대로 남겨뒀습니다. 새로 만들어지는 탭에는
쓰이지 않습니다.

원본 시트는 사진 한 장당 한 행씩 계속 쌓이는 로그 형태이고, 이 시트는 그
데이터를 **제품군(예: 올리브유) 단위로 묶어서 상품끼리 나란히 비교**할 수
있게 정리한 파생 뷰입니다. 원본 시트를 건드리지 않으므로, 이 시트의 분류가
잘못돼도 원본과 대조해서 언제든 다시 정리할 수 있습니다.

**구조** (카드형 - 상품 하나가 열 하나):

```
[제품군명]                         <- 제목 행
사진        (비어있음, 수기 채움용)
상품명      DR.G 닥터지 선크림      이니스프리 선크림
소싱형태    (비어있음)
매출(연)    (비어있음)
규격/단량   150ml                  50ml
판매가      26,990원               15,000원
단위단가    (비어있음)
매총율      (비어있음)
산도        (비어있음)
병입원산지  (비어있음)
셀링        - 자외선 차단 ...       - 산뜻한 사용감 ...

                                   <- 2행 공백
[다음 제품군명]
...
```

- OCR 상품카드에서 뽑히는 정보(상품명/규격·단량/판매가/단위단가/셀링)가
  채워지고, **소싱형태는 매칭된 제품 뒷면 사진에서 자동으로 채워집니다**
  (아래 별도 설명 참고). **병입원산지**는 두 경로로 채워질 수 있습니다:
  ① 상품카드 자체에 "원산지:"가 직접 찍혀 있으면("170G원산지영국"처럼
  중량 줄 끝에 붙어 나오는 경우 포함) 뒷면 사진 없이도 카드 처리 시점에
  바로 채워지고, ② 카드 자체엔 원산지 표기가 없지만 매칭된 뒷면 사진이
  있으면 그 뒷면에서 채워집니다(①이 더 먼저 채워지므로, 뒷면이 나중에
  매칭돼도 카드 자체의 표기를 덮어쓰지 않습니다). 나머지 행(사진/매출(연)/
  매총율/산도)은 항상 빈 칸입니다 — 수기로 채우거나 다른 소스에서 채워
  넣는 몫입니다. 상품코드/파일ID/원문텍스트는 이 시트에는 없습니다 — 이
  시트는 사람이 보기 좋은 정리본이고, 그 추적용 정보는 원본(RAW) 시트에
  사진 한 장당 한 행씩 이미 남아있습니다.
- **규격/단량은 독립된 줄뿐 아니라 제품명 끝에 붙어 나온 경우도 자동으로
  분리합니다** ("오리온 오그래놀라 시나몬츄러스 440g* 3개" → 제품명 "오리온
  오그래놀라 시나몬츄러스" + 규격/단량 "440g* 3개").
- **셀링(셀링포인트)은 OCR 원문에서 `-`/`•`로 시작하는 문구를 뽑아 정리합니다**
  (`extract_selling_points()`). 화면 폭 때문에 한 문구가 여러 줄로 잘려
  나오면("- 신선하고 품질이 좋은 ... 압착하여" 다음 줄에 "만든 오일") 다음
  `-`가 나오기 전까지는 같은 문구로 이어붙입니다. 단, 가격/코드/영문제품명/
  "신세계포인트"·"적립"·"행사기간" 같은 정형 프로모션 문구가 중간에 끼면
  거기서 문구를 끊습니다 — 아직 완벽하지 않은 초기 버전이라 실사진으로
  계속 검증하며 다듬어야 합니다.
- **제품군 판별은 2단계입니다** (`detect_category()`):
  1. **자사 카탈로그 매칭** (`MC_CATALOG_SPREADSHEET_ID` 설정 시, 1순위):
     비공개 카탈로그 시트의 `상품명`들과 OCR 상품명 사이에서 겹치는 부분
     문자열을 찾습니다. 표기가 다 달라서(예: 카탈로그의 "OO브랜드 아보카도
     오일 1L"과 OCR의 "피에트로코리첼리 아보카도오일 1L(퓨어)") 완전일치가
     아니라 부분 문자열 겹침으로 찾되, 안전하게 매칭하기 위해 두 가지
     조건을 둡니다:
     (a) **가장 길게** 겹치는 부분 문자열부터 확인하고 (짧고 흔한 단어일수록
     오판 위험이 크므로), (b) 그 부분 문자열을 포함하는 카탈로그 항목들의
     제품군(MC)이 **한쪽으로 확실히 쏠려 있을 때만**(과반 이상, 최소 2건
     근거) 매칭을 확정합니다. 예를 들어 "아보카도"라는 흔한 단어만 겹치고
     카탈로그 안에서 "아보카도오일"/"아보카도"(과일) 등 여러 제품군에
     흩어져 있으면 억지로 어느 한쪽으로 정하지 않고 다음 단계로 넘어갑니다
     — 틀리게 분류되는 것보다 미분류로 남는 편이 낫다는 원칙입니다.
  2. **`CATEGORY_KEYWORDS` 수동 목록** (1단계에서 매칭 실패 시, 또는 카탈로그
     자체가 설정 안 돼있을 때): 상품명(한국어)에 이 키가 포함되어 있는지로
     판단하고, 매칭되면 매핑된 제품군 이름을 씁니다. 대부분은 키와 제품군
     이름이 같지만("올리브유"->"올리브유"), 제품명에 제품군을 나타내는
     글자가 아예 없는 경우를 위해 브랜드/제품종류 -> 실제 제품군 매핑도 같이
     둘 수 있습니다("골드베렌"->"구미젤리") — 다만 이런 브랜드들은 대부분
     카탈로그에 이미 있어서 카탈로그가 설정돼있으면 거의 쓰일 일이
     없습니다.

  두 단계 다 실패하면 `미분류` 블록에 모이므로 데이터가 유실되진 않습니다.
  카탈로그 데이터 자체의 `MC` 값에 거래처/공급사명 같은 게 섞여 있으면
  가끔 예쁘지 않은 제품군 이름이 나올 수 있는데, 이건 매칭 로직의 버그가
  아니라 카탈로그 데이터 품질 문제라 카탈로그를 정리하거나 수동으로
  고쳐줘야 합니다.
- 같은 상품코드가 다시 나와도(재촬영 등) 항상 새 열을 추가합니다 — 기존
  열을 덮어쓰지 않습니다.
- 병렬로 OCR 처리된 상품들을 시트에 배치하는 작업(제품군 블록에서 다음
  빈 열 번호를 정하는 것)은 여러 스레드가 동시에 하면 충돌하므로, 모든
  OCR이 끝난 뒤 한 번에 단일 스레드로 처리합니다.
- 이 시트는 아직 실사진으로 폭넓게 검증되지 않은 초기 버전입니다. 특히
  제품군 키워드 목록이 지금은 이 세션에서 실제로 봤던 몇 가지(선크림,
  올리브유, 구미젤리 등)만 들어있어서, 대부분의 사진이 처음엔 `미분류`로
  모일 수 있습니다.

### 소싱형태 / 병입원산지 자동 판별

상품카드와 매칭된 제품 뒷면 사진(`매칭파일ID`, 6절 참고)이 있으면
`determine_sourcing()`이 그 뒷면 원문텍스트를 읽어 소싱형태/병입원산지를
자동으로 채웁니다.

**소싱형태** (`PB`/`직수입`/`벤더구매` 중 하나, 또는 빈 칸):
1. **PB**: 상품카드 제품명에 "T Standard"/"티스탠다드" 또는 "kirkland"/
   "커클랜드" 계열 문구가 있으면 무조건 PB (판단 기준은 뒷면이 아니라
   상품카드 제품명입니다).
2. **직수입 (상품카드 자체 인증 문구)**: 코스트코가 가격표에 "-직수입
   오리지널 제품"처럼 "직수입" 문구를 직접 불릿으로 찍어두는 경우가
   실사진(도리토스 나초칩)에서 확인됐습니다 - 뒷면의 라벨을 해석해서
   추론하는 게 아니라 코스트코 스스로 매긴 소싱 인증이라, **뒷면 사진이
   아예 없어도** 이 값만으로 직수입이 채워집니다.
3. **직수입 (뒷면 라벨 기반)**: 뒷면의 수입원/수입자/수입업체/수입판매업소
   (접미사 없이 그냥 "수입"만 적힌 경우도 인식합니다 - 코스트코 자체
   라벨에 흔한 표기입니다)가 그 사진을 찍은 매장의 유통 계열과 일치하면
   직수입입니다. 트레이더스는 이마트 계열이라 "이마트"가 적혀있어야
   직수입이고, 코스트코 사진엔 "코스트코"가 적혀있어야 직수입입니다 —
   트레이더스 사진에 수입업체가 "코스트코"로 적혀있는 경우처럼 서로
   어긋나면 그 매장 입장에서는 직수입이 아닙니다(아래 4번으로). "반품 및
   교환장소: 구입처 및 수입판매업소"처럼 식품 표시 고시 문구에 관용적으로
   들어가는 "수입판매업소"는 실제 수입사 라벨이 아니므로 여기서 걸리지
   않습니다.
4. **벤더구매**: 위 경우가 아니면서 제조사(제조원/제조자/제조업자 등)와
   수입사가 뒷면에 둘 다 적혀있으면 벤더구매입니다.
5. 위 경우에 다 해당하지 않으면(=상품카드에 직수입 인증도 없고 뒷면에
   수입 경로도 안 적혀있으면) 소싱형태는 비워둡니다. 뒷면 사진 자체가
   없고 상품카드에도 PB/직수입 근거가 없으면, 소싱형태를 계산해봐야 항상
   빈 값만 나오므로(그리고 원산지도 "정보 없음"을 "국내"로 잘못 단정하게
   되므로) 애초에 계산을 건너뜁니다 - "확인 안 됨"과 "국내산으로 확인됨"은
   다른 뜻이기 때문입니다.

**병입원산지** (제조국): "원산지:"/"제조국:"/"Place of origin:" 라벨이나
"Made in X" 표기를 최우선으로 쓰고, 없으면 제조자/제조원 주소 근처의
국가명을 찾습니다. 아무 단서도 못 찾았고 위 소싱형태 판별도 4번(수입 경로
없음)으로 빠졌다면 국내 생산으로 보고 "국내"를 채웁니다 — 다만 원산지
표기 자체는 있는데 수입 라벨만 없는 경우(예: "책임판매업자"만 있고
"수입원"은 없는 화장품 라벨)엔 그 원산지 표기 값을 그대로 쓰고 "국내"로
덮어쓰지 않습니다.

**뒷면이 한글 표기 없이 영문 라벨만 있는 수입 완제품**(예: 해외 브랜드
치즈/크림류)도 인식합니다 - "Manufacturer:"/"Manufactured by"는 제조사,
"Imported By:"/"Importer"/"Distributed by"/"Distributor"는 수입사,
"Place of origin:"/"Country of origin:"은 병입원산지 라벨로 각각 한글
표기와 동등하게 처리됩니다. 뒷면 판별(`is_back_label()`) 자체도 한글
법정 표기가 하나도 없이 이런 영문 표기만 있어도 뒷면으로 인식합니다.

카드와 뒷면이 서로 다른 실행 회차에 올라와도(뒷면을 나중에 올리는 경우)
`sync_back_sourcing()`이 RAW 시트 전체 이력을 다시 훑어 매칭하고, 그 카드의
`정리본위치`를 찾아 소급 반영합니다. 다만 **이미 값이 채워진 칸은 절대
덮어쓰지 않습니다** — 자동 판별이 틀렸을 때 사람이 수기로 고쳐두면 다음
실행 때도 그 수정이 유지됩니다.

## 8. 정확도 향상 팁

- Azure가 반환하는 줄 순서는 항상 사진의 실제 위→아래 순서와 일치하지는 않습니다
  (특히 상품코드/바코드 구역과 제품명/가격 구역이 서로 떨어져 있는 가격표 사진에서
  두드러짐). `ocr_image_azure()`가 각 줄의 bounding box 좌표로 자동 재정렬하므로
  대부분은 문제없지만, 그래도 이상하게 파싱되는 사진이 있으면 해당 행의 `원문텍스트`를
  확인해서 실제 줄 순서와 비교해보세요. 셀링포인트(특징 문구)가 카드 하단에 좌/우
  2단으로 나란히 배치된 레이아웃도 `_reorder_two_column_block()`이 폭이 좁은
  줄들을 좌/우로 클러스터링해 "왼쪽 단을 전부 읽은 뒤 오른쪽 단"의 순서로
  재배열해줍니다 - 뚜렷한 2단 구분(간격이 카드 폭의 15% 이상)이 안 보이면
  아무 것도 바꾸지 않고 원래 순서를 유지합니다.
- 브랜드명이 "RICOLA", "MIKAKUTO"처럼 대문자로 단독 줄에 찍히는 경우가 많아서,
  `parse_price_fields()`의 영어 제품명 판별 조건은 "대문자 2글자 이상 연속 +
  2단어 이상"을 요구합니다. 브랜드명 한 단어로는 걸리지 않게 하기 위함입니다.
  배경에 다른 상품이 같이 찍히면 그 포장의 작은 글자 파편이 "HEE"/"CON"처럼
  뜻 없는 짧은 대문자 낱말 줄로 OCR되어 진짜 브랜드명 줄 앞에 끼어드는 경우도
  있는데, 한글 제품명 시작 전에 이런 짧은 대문자 낱말이 2개 이상 연달아 나오면
  노이즈로 보고 한글 바로 앞의 마지막 한 줄만 브랜드명으로 남깁니다.
- 사진 촬영 시 빛 반사를 피하는 것이 어떤 OCR 엔진보다 정확도에 큰 영향을 줍니다:
  정면보다 살짝 비스듬히, 확산광(자연광)에서, 플래시 직사 없이 촬영하는 것을 권장합니다.
- 배경 진열대의 다른 가격표가 프레임에 같이 걸려도 시트에는 항상 한 행만
  추가되므로 걱정하지 않으셔도 됩니다(위 5절 참고). 다만 목표 가격표를 최대한
  크고 선명하게 찍을수록 그 한 행의 인식 정확도는 높아집니다.
- **사진에 다른 물체(옆 상품 박스, 뒤의 다른 가격표 등)가 같이 찍혀서 그
  텍스트까지 섞여 들어오는 문제를 줄이기 위해, 실제 사진 픽셀에서 배경색을
  뽑아 세로 줄 간격과 함께 레이아웃을 분석**합니다(`_cluster_lines_by_layout()`).
  같은 상품카드 안의 줄들은 촘촘하고 배경색도 일정하다고 보고, 간격이 크게
  벌어지거나(다른 물체로 전환) 간격이 살짝 벌어지면서 배경색도 확 다르면
  그 지점에서 나눠서, 가장 크게 찍힌(=실제 촬영 대상일 가능성이 높은) 덩어리만
  남깁니다. 이 분석은 실제 이미지 픽셀을 열어서 하는 거라 텍스트 기반
  수정들과 달리 **실사진으로만 진짜 정확도를 검증할 수 있습니다** - 두
  물체가 화면에서 거의 붙어있어서 간격 차이가 거의 없는 경우(예: 상품 박스
  바로 위에 가격표가 붙어있는 구조)는 이 방식으로도 완전히는 못 걸러낼 수
  있습니다. 이미지를 열지 못하거나 분석 중 오류가 나면 조용히 건너뛰고
  기존 방식(필터링 없음)으로 계속 진행합니다.
- **`parse_traders_fields()`는 실사진 검증이 아직 부족한 초기 버전입니다.**
  트레이더스 가격표 사진 1장(레이아웃 설명)만으로 만든 파서라, 코스트코
  파서가 겪었던 것과 같은 종류의 실사진 기반 버그(줄 순서, 브랜드명/단가
  오인식 등)가 나올 가능성이 높습니다. 트레이더스 사진을 실제로 여러 장
  돌려보고, `원문텍스트` 대비 잘못 뽑힌 필드가 있으면 코스트코 때와 같은
  방식으로 하나씩 고쳐나가야 합니다.

## 9. 헤더가 예상과 다를 때

시트 1행의 헤더가 위 컬럼 순서와 다르면, 자동으로 안전하게 처리 가능한 경우
(컬럼이 끝이나 중간에 새로 추가된 경우)만 알아서 맞추고, 그 외(순서 변경/삭제)는
경고만 출력하고 **기존 데이터는 지우지 않습니다**. 이 경우 시트 1행을 위 컬럼
순서에 맞게 직접 수정해주세요.

## 10. 정리본 자동 재검토 (`review_and_fix.py`)

OCR을 새로 돌리지 않고, **이미 시트에 저장된 원문텍스트를 최신 파싱 코드로
다시 읽어서** 정리본(제품군정리)의 빈 칸을 채우고 뒷면 오분류를 바로잡는
별도 도구입니다. `main.py`(신규 사진 OCR)와는 완전히 분리된 스크립트라
Drive나 Azure에는 전혀 접근하지 않고, 그래서 비용도 들지 않습니다.

**하는 일 (전부 "덧붙이기"만 하고, MD가 손댔을 수 있는 값은 절대 덮어쓰지
않습니다):**
- 정리본에서 비어있는 칸(규격/단량, 판매가, 단위단가, 셀링)을 재파싱 결과로
  채웁니다. 이미 값이 있는 칸은 건드리지 않습니다.
- RAW의 핵심 필드(상품코드/제품명(한국어)/가격/제품명(영어)/중량/단가)는
  재파싱 결과가 다르면 최신값으로 갱신합니다 - RAW는 원문텍스트에서 파생되는
  자리라 사람이 손대는 곳이 아니라서, 코드가 좋아지면 값도 같이 갱신하는 게
  맞다고 보고 이 필드들만 예외적으로 덮어씁니다.
- `is_back_label()`로 다시 판별했을 때 뒷면으로 바뀐 상품은 정리본에서
  제거합니다.
- 정리본의 상품명 칸은 RAW의 제품명(한국어)을 그대로 비추는 자리라, 재파싱
  으로 RAW 이름이 바뀌면(예: 끝에 붙어있던 규격이 새로 분리되는 경우) 정리본
  이름도 같이 동기화합니다.

**못 하는 일:** 사진 자체가 잘못 읽힌 경우(재파싱으로도 필드가 안 채워지는
경우)는 이미지 재OCR이 필요해서 이 도구로는 못 고치고, 실행 결과 리포트에
"재확인 필요" 목록으로만 남습니다. RAW의 제품명(한국어) 자체가 완전히
다른 값으로 바뀐 경우(위의 "규격 분리" 패턴이 아닌 경우)도 이름 대조가
끊겨서 자동으로는 못 잡고 "대조 못 함" 목록에 남습니다 - 이런 항목은 지금도
사람이 직접 확인해야 합니다.

**실행 방법:**
```bash
python review_and_fix.py          # 실제로 시트에 반영
DRY_RUN=true python review_and_fix.py   # 무엇을 고칠지만 로그로 확인 (시트는 안 건드림)
```
`.github/workflows/review-and-fix.yml`로도 등록되어 있어 Actions 탭에서
수동 실행하거나(`dry_run` 입력값으로 점검 모드 선택 가능), `auto-ocr.yml`의
"업데이트" 버튼과 같은 방식으로 대시보드에 별도 버튼을 연결해 트리거할 수
있습니다(연동 방법은 `DASHBOARD_INTEGRATION.md` 참고 - 같은 `workflow_dispatch`
패턴입니다). 신규 사진 OCR과 같은 스프레드시트를 다루므로, 두 워크플로는
같은 concurrency 그룹을 공유해서 동시에 돌지 않습니다.

## 11. AI 분류 제안 / 월간 SKU 비교 (Gemini, 선택 기능)

`GEMINI_API_KEY`를 설정하면 두 가지 AI 보조 기능을 쓸 수 있습니다. 둘 다
**선택 기능**이라 키가 없으면 조용히 건너뛰고 끝나며, OCR 파이프라인
자체(`main.py`)와는 완전히 분리되어 있어 이 기능에 문제가 생겨도 신규 사진
처리에는 영향이 없습니다.

**공통 필요 시크릿:** `SPREADSHEET_ID`, `GOOGLE_SERVICE_ACCOUNT_JSON`(이미
등록되어 있는 것 그대로 재사용), `GEMINI_API_KEY`(신규 - [Google AI
Studio](https://aistudio.google.com/apikey)에서 무료로 발급, 카드 등록 불필요).

### 11-1. 미분류 상품 AI 분류 제안 (`classify_unclassified.py`)

카탈로그 매칭(`MC_CATALOG_SPREADSHEET_ID`) + `CATEGORY_KEYWORDS`(main.py 수동
목록)로 못 잡아서 "미분류" 블록에 쌓인 상품들을 Gemini에게 보여주고 제품군을
제안받습니다. **원본 정리본 시트는 건드리지 않고**, `AI분류제안_<원본탭이름>`이라는
새 탭에 상품명·제안 제품군·확신도·사유를 적어두기만 합니다 - 제품군 블록을
실제로 옮기는 건 되돌리기 번거로운 작업이라 자동으로 하지 않고, 사람이 검토
후 필요하면 `CATEGORY_KEYWORDS`나 자사 카탈로그 시트에 직접 반영하는 방식을
권장합니다(그렇게 채워나갈수록 다음 달부터는 미분류 비중이 줄어듭니다).

```bash
python classify_unclassified.py --retailer costco   # 그 리테일러의 가장 최근 정리본 탭 자동 감지
python classify_unclassified.py --sheet "정리본(코스트코)_2026-07"   # 특정 탭 지정
```

### 11-2. 월간 SKU 비교 + AI 종합 해석 (`monthly_comparison.py`)

정리본 탭이 2개월치 이상 쌓이면, 이전 달과 이번 달을 비교해서 전체 SKU
증감·대분류(식품/비식품)·중분류·소싱형태·가격 변화를 계산하고, 그 숫자를
Gemini에 보내 종합 해석 문단을 받아옵니다. **숫자 계산은 전부 파이썬 코드가
결정적으로 수행하고, Gemini는 그 숫자를 해석하는 글쓰기만 담당**합니다(AI가
직접 숫자를 세거나 계산하지 않으므로 계산 오류나 매번 다른 숫자가 나올
걱정이 없습니다). 결과는 `AI분석_<이전탭>_vs_<이번탭>` 새 탭에 기록되고,
원본 정리본 탭은 건드리지 않습니다.

이 비교는 상품코드 없이 상품명 텍스트 유사도로 동일 SKU를 찾는 방식이라
(`product_matching.py`), 브랜드 한글/영문 중복 표기나 OCR 오타 때문에 다른
문자열이 된 동일 상품을 놓칠 수 있습니다. 새로운 패턴을 발견하면
`product_matching.py`의 `BRAND_ALIAS_PAIRS`/`SPELLING_FIX_PAIRS`/
`MANUAL_CANONICAL_ALIASES`에 추가해 정확도를 계속 높여나가야 합니다. 또한
이 데이터는 매대 전수조사가 아니라 그때그때 촬영한 사진 기록이므로, "제외/신규"
수치는 실제 단종/신규 도입이 아니라 촬영 커버리지 차이를 반영할 수 있다는 점을
해석 시 항상 감안해야 합니다(Gemini 프롬프트에도 이 유의사항을 포함해뒀습니다).

```bash
python monthly_comparison.py --retailer costco   # 가장 최근 2개월 탭 자동 감지
python monthly_comparison.py --retailer costco --prev "정리본(코스트코)_2026-06" --curr "정리본(코스트코)_2026-07"
```

### 11-3. 자동 실행

`.github/workflows/ai-analysis.yml`이 매달 1일(KST 09:10)에 코스트코/트레이더스
둘 다 자동으로 돌립니다(이번 달 정리본이 없거나 비교할 두 달치가 안 쌓였으면
스크립트가 조용히 건너뛰므로 매달 도는 것 자체는 안전합니다). Actions 탭에서
수동 실행도 가능하고, 두 기능(분류 제안/월간 비교)을 따로 켜고 끌 수 있습니다.
